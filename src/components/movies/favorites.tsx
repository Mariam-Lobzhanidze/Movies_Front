import UserMovieList from "../shared/userMovieList";

const FavoriteMovies: React.FC = () => {
  return <UserMovieList listType="favorites" title="Favorite Movies" />;
};

export default FavoriteMovies;
